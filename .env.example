# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# MongoDB Configuration
# For local MongoDB without auth:
# MONGODB_URI=mongodb://localhost:27017/shortlink-bot
# For MongoDB with authentication:
MONGODB_URI=mongodb://username:password@host:port/database?authSource=admin
# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/shortlink-bot?retryWrites=true&w=majority

# Server Configuration
# For local development:
PORT=3000
BASE_URL=http://localhost:3000

# For production with reverse proxy (recommended):
# PORT=3000
# BASE_URL=https://yourdomain.com

# For Cloudflare Tunnel (home server deployment):
# PORT=3000
# BASE_URL=https://yourdomain.com
# Note: Make sure to set 'trust proxy' to true in your app

# For cloud platforms (Heroku, Railway, etc.):
# PORT=${PORT:-3000}
# BASE_URL=https://yourapp.herokuapp.com

# For direct HTTPS (requires SSL certificates):
# PORT=443
# BASE_URL=https://yourdomain.com

# Environment
# Set to 'production' for production deployment
NODE_ENV=development
